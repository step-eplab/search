{

# createPDG.py
"run_proc": true,	[true, false]	запускать процессы или нет (false нужно для отладки кода) 
"multi_proc": false,	[true, false]	использовать multiprocessing или нет (при отработке всегда false)

"dir_LC": "/home/test/step/data/F1/lightcurves/v6/",		путь директории с кривыми блеска, в которой должна быть папка SLC/SLC_{ver_slc} (ver_slc см. ниже)
"dir_res": "/home/test/step/results/F1/v6/",			путь директории для сохранения всех результатов
"cat_path": "/home/test/step/data/F1/catalogs/F1_24.list",	['', {path}] путь к опорному каталогу, нужен при использовании mag_max (если '', то mag_max не учитывается)

"ver_slc": "_2",	номер версии используемых кривых блеска для поиска SLC (Search LC), которые расположены в SLC/SLC_{ver_slc} (нужно устанавливать уникальное значение при создании новых версий кривых блеска SLC)
"ver_search": "_4",	номер версии поиска, будет создана папка для сохранения результата dir_res/search/{ver_search} (нужно устанавливать уникальное значение при изменении параметров поиска 

### Параметры поиска
"min_dt": 120,		минимальная длина кривой блеска в часах
"min_N": 1000,		минимальная длина кривой блеска в количестве точек
"mag_max": 14,		ограничение по максимальной звездной величине (если cat_path != '')

"Pers_BLS": [[25, 100, 0.5]],	диапазоны сетки периода для построения периодограмм с помощью BLS (Box Least Squares) в формате [[p1_1, p2_1, dp_1], [p1_2, p2_2, dp_2], ..., [p1_n, p2_n, dp_n]], где p1_i, p2_i, dp_i -- минимальный, максимальный период и шаг сетки для i-го диапазона (i от 1 до n)
"Durs_BLS": [1, 5, 0.5],	диапазон сетки длительности транзита для BLS
"Pers_LS": [[0.1, 7.5, 0.005], [8.5, 12, 0.01], [12, 24, 0.05], [24, 100, 0.1]],	диапазон сетки периода для LS (Lomb-Scargle), формат аналогично Pers_BLS 

"reg": "BLS",	["BLS", "LS", "both"]	режим поиска

"G_del": [],		индексы по опорному каталогу, для которых не нужно строить периодограмму
"G_select": [],		индексы по опорному каталогу, только для которых нужно строить периодограмму
"N_g_inx": 50,		количество кривых блеска, для которых нужно строить периодограмму


# procPDG.py
"ver_cor": "_cor1",	версия обработки периодограммы (нужно устанавливать уникальное значение при изменении параметров обработки периодограмм) 
### Параметры обработки периодограмм
"P_min": 25,				минимальный период
"P_max": 100,				максимальный период
"P_del": [24, 12, 8, 6],		значения периода, кратным которым значения из сетки периода BLS будут удалены в диапазоне P_del_range соотвественно, должно выполняться len(P_del)=len(P_del_range)
"P_del_range": [1, 0.5, 0.25, 0.1],	диапазоны периода вокруг значений сетки периода BLS, кратных P_del соотвественно, должно выполняться len(P_del)=len(P_del_range)

# selectPDG.py
"d_rel": 0.1,		максимальный остаток при делении рассматриваемого периода к периодам, соответстующим пикам выборки N_peak
"N_peak": 10,		количество периодов, соответствующих отсортированным по убыванию мощности пикам периодограммы (1 -> абсолютный максимум, 2 -> два самых значимых пика и т.д.)
"N_per": 5,		количество рассматриваемых периодов выборки N_peak (N_per <= N_peak)

# searchOI.py
"reg_search": "G",	["G", "S", "both"]
### Режим отбора G (по повторяемости)
"N_max": 500,		максимальное количество звезд в группе, которым соответствует одинаковая повторяемость n периода для разных вариантов кривых в SLC  (пока не учитывается)
"n": 3,			рассматриваемое количество повторений периода (повторяемость) для разных вариантов кривых в SLC 
### Режим отбора S (по величине)
"v_min": 11		минимальный уровень SNR для отбора
}

Сейчас не создаются SLC кривые (createSLC.py не запускать)

Сохранять формат при указании директорий (в конце '/')

Принцип отбора selectPDG: среди N_peak максимальных пиков на всей периодограмме рассматриваются N_per первых пиков, для каждого из которых определяется количество кратных периодов выбранному среди периодов выборки N_peak с точность d_rel  

Принцип отбора searchOI: из таблицы в таблице res_BLS{ver_search}{ver_cor}.csv выбираются те индексы, которым соответсвует условие отбора в зависимости от выбранного режима G или S.

Вариант G: все периоды группируются по количеству повторений среди разных версий кривых блеска в SLC для каждой звезды, среди этих групп рассматриваются те звезды с соответствующими периодами, для которых повторяемость равна n

Вариант S: рассматриваются звезды с теми периодами, для которых SNR > v_min
